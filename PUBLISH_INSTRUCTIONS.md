# 📦 Инструкция по публикации ChatList на GitHub

## Быстрый старт

### 1. Подготовка репозитория

```bash
# Убедитесь, что вы в корне проекта
cd C:\Work\ChatList

# Добавьте все файлы (если еще не добавлены)
git add .

# Создайте коммит
git commit -m "Prepare for release v1.0.0"

# Отправьте на GitHub
git push origin main
```

### 2. Создание релиза (вручную)

#### Шаг 1: Обновите версию
1. Откройте `version.py`
2. Измените версию (например, `1.0.0`)
3. Сохраните

#### Шаг 2: Соберите приложение
```bash
# Соберите exe
pyinstaller ChatList.spec --clean

# Создайте установщик
"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" ChatList.iss
```

#### Шаг 3: Создайте тег
```bash
git add version.py
git commit -m "Bump version to 1.0.0"
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
```

#### Шаг 4: Создайте Release на GitHub
1. Перейдите: https://github.com/yourusername/ChatList/releases/new
2. Выберите тег: `v1.0.0`
3. Заголовок: `ChatList v1.0.0`
4. Описание: Скопируйте из `RELEASE_NOTES.md`
5. Загрузите файл: `installer\ChatList-Setup-1.0.0.exe`
6. Нажмите "Publish release"

### 3. Настройка GitHub Pages

#### Вариант 1: Через веб-интерфейс
1. Перейдите: Settings → Pages
2. Source: Deploy from a branch
3. Branch: `main` → `/docs`
4. Save

#### Вариант 2: Через отдельную ветку
```bash
# Создайте ветку gh-pages
git checkout -b gh-pages
git rm -rf .
git checkout main -- docs/
git mv docs/* .
git commit -m "Setup GitHub Pages"
git push origin gh-pages
git checkout main
```

Затем в Settings → Pages выберите:
- Branch: `gh-pages`
- Folder: `/ (root)`

### 4. Обновление ссылок в документации

**Важно!** Замените `yourusername` на ваш GitHub username в следующих файлах:

1. `docs/index.html` - все ссылки на GitHub
2. `RELEASE_NOTES.md` - ссылки на релизы
3. `README.md` - ссылки на репозиторий (если есть)

Используйте поиск и замену:
- `yourusername` → `ваш_username`

## Автоматизация (опционально)

Если хотите автоматизировать процесс:

1. Файл `.github/workflows/release.yml` автоматически соберет и опубликует релиз при создании тега
2. Файл `.github/workflows/pages.yml` автоматически обновит GitHub Pages при изменении файлов в `docs/`

Для работы автоматизации:
- Убедитесь, что workflows включены в Settings → Actions
- При создании тега релиз будет создан автоматически

## Структура файлов

```
ChatList/
├── .github/
│   └── workflows/
│       ├── release.yml      # Автоматизация релизов
│       └── pages.yml        # Автоматизация GitHub Pages
├── docs/                    # Файлы для GitHub Pages
│   ├── index.html          # Главная страница
│   ├── assets/
│   │   └── style.css       # Стили
│   └── .nojekyll           # Отключение Jekyll
├── installer/              # Готовые установщики
│   └── ChatList-Setup-*.exe
├── RELEASE_GUIDE.md        # Подробное руководство
├── RELEASE_NOTES.md        # Шаблон заметок о релизе
└── PUBLISH_INSTRUCTIONS.md  # Этот файл
```

## Чеклист перед публикацией

- [ ] Версия обновлена в `version.py`
- [ ] Приложение собрано и протестировано
- [ ] Установщик создан и протестирован
- [ ] Release notes подготовлены в `RELEASE_NOTES.md`
- [ ] Все ссылки обновлены (заменен `yourusername`)
- [ ] Тег создан и отправлен
- [ ] Release создан на GitHub
- [ ] GitHub Pages настроен и работает
- [ ] Лендинг проверен и выглядит хорошо

## Полезные команды

```bash
# Просмотр всех тегов
git tag

# Удаление тега (если нужно)
git tag -d v1.0.0
git push origin :refs/tags/v1.0.0

# Просмотр последнего релиза
git describe --tags --abbrev=0
```

## Поддержка

Если возникли проблемы:
1. Проверьте логи GitHub Actions (если используете автоматизацию)
2. Убедитесь, что все файлы закоммичены
3. Проверьте права доступа к репозиторию
